Collection/skills-era scripts (deploy-collection, publish-collection, seed-skills).
Superseded by `memory init --register` and `memory push` in engine/cli.
Not wired into package.json; kept until the pivot is committed.
